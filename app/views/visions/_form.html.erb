<% @vision.errors.full_messages.each do |msg| %>
  <div class="alert alert-danger" role="alert">
    <%= msg %>
  </div>
<% end %>

<%= form_for @vision do |form| %>
  <div class="row">
    <div class="col-sm-8">
      <div class="form-group">
        <%= form.label "Para onde eu vou?" %>
        <%= form.text_area :where_im_going, autocomplete: "off", class:"form-control resize-none" %>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-sm-8">
      <div class="form-group">
        <%= form.label "Onde quero chegar?" %>
        <%= form.text_area :where_arrive, autocomplete: "off", class:"form-control resize-none" %>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-sm-8">
      <div class="form-group">
        <%= form.label "Como posso fazer para tornar concreta a minha missão?" %>
        <%= form.text_area :how_complete_mission, autocomplete: "off", class:"form-control resize-none" %>
      </div>
    </div>
  </div>
  <%= form.submit texto_da_acao, class:"btn btn-primary" %>
<% end %>
