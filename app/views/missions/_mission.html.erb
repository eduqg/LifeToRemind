<tr>
  <td><%= mission.purpose_of_life %></td>
  <td><%= mission.who_am_i %></td>
  <td><%= mission.why_exist %></td>
  <td><%= mission.created_at.strftime("%d/%m/%Y") %></td>

  <td><%= link_to "Selecionar", plans_update_selected_mission_path(mission.id),
                  class:"btn btn-primary"
          %></td>
  <td><%= link_to "Editar", edit_mission_path(mission),
          class:"btn btn-default"
          %></td>
  <td><%= link_to "Remover", mission, method: :delete,
          class:"btn btn-danger",
          data: {confirm: "Tem certeza que deseja remover essa missão? #{mission.purpose_of_life}" }
          %></td>
</tr>