<div class="row">
  <div class="col-border">
    <div class="card">
      <div class="card-body">
        <div class="padding-all">
          <h5 class="card-title"><%= goal.name %> </h5>
          <% goal_data = [["", goal.progress ]] %>
          <%= bar_chart goal_data, suffix: "%", label: "Porcentagem concluída",  min: 0, max: 100, height: "70px" %>
        </div>
      </div>
    </div>
  </div>
</div>